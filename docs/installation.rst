============
Installation
============

At the command line::

    $ easy_install jetstream.cli

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv jetstream.cli
    $ pip install jetstream.cli